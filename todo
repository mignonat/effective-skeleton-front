- Put client and server source code in a directory src
- Add concat-app and concat-app-translations in gulp start-front-prod task
- Create a langage key const module for server
- Create a langage module witch read in translation propreties file following the locale
- Put translations in properties files
- Client get all keys from a const module templating it in index.html or use gulp to create langage.js
- Set unit tests and selenium
- Securize token => not use a simple hash, put in the token an object without user password
- Mongodb look to how to reconnect automatically
- Scalable asynch notification & alert system
- Sys Admin management : show log, manage users and groups, manage maintenance
- Paypal payments
