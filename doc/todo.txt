------ LAURENT --------
- Event : remove handlers when component is destroyed
- CSS Form : refactoring of css classes and div, use of em
- Form : undo/redo history
- Refactor contextual transition : use javascript height transition
- CSS Form : manage errors and help (with javascript)
- Add visual extensions to Flo : git blame, git history, vetur
- Create a component to display non persistent messages 
- Add sample page with all icons available
- Create a list component : pagination, properties to display, hide/show column, sort, etc ...
- Possibility to set an icon for popup : warning, info, error
- Manage responsive design (at least for top bar)
- Try to remove npm global install dependencies for deployment
- All input : test params injection with owasp in ajax util for all parameters
- Define minimum version of browser then check & block old browser
- Webpack : smart caching, vendors and uglify (not removed licenses), HTML5 app manifest
- Possibility to change properties and restart the app
- Make selenium and unit test for each functionality

- Manage Users & groups
- Consult logs file (and search on it)
- Schedule and manage maintenance mode
- Schedule and manage batch

------ FLORIAN --------
- Separer les elements du formulaire d'exemple en composants
- finir la popup 
- définir des medias queries
- rajouter les transitions @-webkit-keyframes
- reprendre les noms css de la 404
