git clone https://github.com/mignonat/healer
	=> initialize the project

npm install
	=> download all the dependencies in package.json

npm run build
    => run babel transpiler and create javascript in public

npm run dev
    => run the app in dev mode (with auto refresh)

npm run prod
    => normal startup
